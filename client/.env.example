# API base URL. Leave unset to use relative /api (recommended: Vite proxy in dev, same-origin in prod).
# Do NOT set to http://localhost:5000/api — that causes ERR_CONNECTION_REFUSED when the backend is not reachable.
# VITE_API_URL=/api
