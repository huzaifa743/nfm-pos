git add .
git commit -m "Fixing both receipt components to derive the VAT-inclusive subtotal from total and VAT % when the stored subtotal is wrong (e.g. old records or legacy data)."
git push origin main
